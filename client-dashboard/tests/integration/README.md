# Integration Tests

This folder contains multi-component and cross-domain integration tests for the dashboard.

Guidelines:
- Cover interactions between hooks, services, and components.
- Verify API error handling and loading states.
- Keep unit-level rendering assertions in `tests/unit`.
